#include "resources.hpp"

namespace Render{

    void Resources::addModel(Model * const model){
        //models.insert(model);
    }

    void Resources::addTexture(const Texture){

    }

    bool Resources::queryModels(const Model * const){
        return false;
    }

    bool Resources::queryTextures(const Texture){
        return false;
    }
}